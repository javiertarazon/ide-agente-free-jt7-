# Memoria de Auto-Correccion

## Proposito
Este archivo evita repetir errores en sesiones futuras. Se actualiza despues de corregir fallos reales.

## Reglas
- Registrar solo lecciones accionables.
- Incluir causa raiz y regla preventiva.
- Referenciar archivo/comando cuando aplique.

## Lecciones Aprendidas
| Fecha | Error Tecnico Detectado | Causa Raiz | Solucion y Regla Preventiva |
| :--- | :--- | :--- | :--- |
| 2026-03-11 | Inicializacion del sistema de memoria persistente | No existia `docs/` para trazabilidad operativa | Regla: crear y leer `docs/TASKS.md`, `docs/MEMORY.md` y `docs/STRATEGY_LOG.md` al iniciar tareas complejas |
| 2026-03-13 | Bucle operativo por tarea heredada en estado `en_progreso` | Ejecucion valida completada sin reconciliar una entrada duplicada en `copilot-agent/tasks.yaml` | Regla: despues de verificar una ejecucion, cerrar o cancelar explicitamente todas las tareas duplicadas relacionadas y actualizar `copilot-agent/RESUME.md` antes de continuar |
| 2026-03-13 | Login MT5 fallaba por parametros vacios o rotos | `mt5.js` usaba `params.arguments` (undefined) e interpolaba strings directo en Python | Regla: pasar `params` directo y enviar JSON por stdin al script Python para evitar valores perdidos e inyeccion |
| 2026-03-13 | `ModuleNotFoundError: tools` al ejecutar MT5 | `mt5.js` apuntaba el `sys.path` al subproyecto y no al repo raiz | Regla: resolver `repoRoot` con `path.resolve(__dirname, \"../../..\")` y usarlo en `sys.path` |
| 2026-03-13 | `mt5.initialize()` colgaba sin fin | No habia timeout en el spawn de Python | Regla: aplicar timeout en `executePythonMT5` y devolver error controlado |
| 2026-03-13 | Credenciales MT5 con comillas rompian login | `.env` trae valores entre comillas y se pasaban tal cual | Regla: normalizar strings (strip de comillas) antes de llamar a MT5 |
| 2026-03-13 | MT5 no respondia hasta login por UI | La terminal estaba abierta pero requerÃ­a login desde el menÃº | Regla: usar `jt7_mt5_desktop_login` con shortcut `%fl` si el login por API se cuelga |
| 2026-03-13 | `jt7_mt5_account_info` fallÃ³ tras login previo | Cada tool corre en proceso nuevo y pierde sesiÃ³n | Regla: auto-login por llamada leyendo `credentials_file` o creds directas antes de operaciones |
| 2026-03-13 | `jt7_mt5_account_info` ignoraba credenciales | El handler no pasaba `params` a `executePythonMT5` | Regla: siempre reenviar `params` en tools sin argumentos para habilitar auto-login |
| 2026-03-13 | `mt5_server.py` no arrancaba con SDK MCP reciente | Uso de API antigua (`callback`, `set_available_tools`) | Regla: adaptar a `@server.list_*()` y `stdio_server` con `server.run()` |
| 2026-03-14 | `AppActivate` de MT5 fallo al usar titulo fijo | La ventana de MT5 usa un titulo dinamico con cuenta/servidor, no siempre contiene "MetaTrader 5" | Regla: detectar `MainWindowTitle` por proceso `terminal64` y usar ese titulo antes de enviar SendKeys |
| 2026-03-14 | El wallpaper no se aplicaba aunque el registro cambiaba | `moto_gp.jpg` era HTML descargado con extension `.jpg`; la API del sistema solo veia un archivo invalido | Regla: validar la firma binaria o cargar la imagen con `System.Drawing` antes de usar cualquier archivo descargado como fondo |
| 2026-03-18 | `add-free-jt7-agent.ps1` no encontraba `skills_manager.py` | El script resolvia rutas relativas desde `scripts/` en vez de usar la raiz del repo | Regla: en wrappers ubicados dentro de subcarpetas, derivar `repoRoot` y resolver binarios/archivos criticos desde esa raiz |
| 2026-03-18 | Instaladores preferian una `.venv` rota y fallaban aunque habia Python funcional | Se validaba solo la existencia del ejecutable, no que pudiera arrancar | Regla: antes de preferir `.venv`, ejecutar una sonda corta (`python -c "import sys"`) y si falla, degradar a `python` o `py -3` |
| 2026-03-18 | `scripts/test-wrappers.ps1` dependia del directorio actual | El test llamaba `.\openclaw-start.cmd` desde la raiz del repo, pero el archivo vive en `scripts/` | Regla: en scripts de prueba, usar `$PSScriptRoot` para invocar archivos hermanos y no asumir el `cwd` |
| 2026-03-18 | Instalaciones paralelas chocaban al escribir `.skills_index.json` | `_atomic_write_text` reutilizaba un `.tmp` fijo y dos procesos podian reemplazar el mismo archivo temporal | Regla: para escrituras atomicas cross-process, usar archivos temporales unicos por PID/UUID y reintentos cortos ante `PermissionError` |
| 2026-03-18 | `npm install` del router Copilot fallaba por version inexistente | `package.json` fijaba `@github/copilot-sdk@^0.0.10`, pero esa rama ya no existe en npm | Regla: validar versiones publicadas con `npm view <paquete> versions --json` antes de fijar dependencias nuevas o migradas |
| 2026-03-18 | Copilot SDK rompia en Node por subpath ESM de `vscode-jsonrpc` | `@github/copilot-sdk` importaba `vscode-jsonrpc/node` y el paquete instalado no exportaba ese subpath para ESM | Regla: cuando una dependencia ESM requiera subpaths no exportados, automatizar el shim/patch en `postinstall` en vez de confiar en un `node_modules` manual |
| 2026-03-18 | El router Copilot seguia bloqueado aunque el runtime ya cargaba | No habia credenciales activas para Copilot CLI/SDK y el mensaje original era poco accionable | Regla: soportar tokens por `COPILOT_GITHUB_TOKEN`/`GH_TOKEN`/`GITHUB_TOKEN` y devolver una instruccion clara de `copilot login` cuando falte autenticacion |
| 2026-03-18 | `npm audit` del root reportaba 10 vulnerabilidades evitables | Habia una dependencia directa vulnerable (`xml2js`), una transitiva fijable (`undici`) y una devDependency antigua (`vscode`) que arrastraba `mocha/minimist` | Regla: primero retirar dependencias legacy no usadas, luego fijar `overrides` puntuales y revalidar con `npm audit --json` |
| 2026-03-18 | El router Copilot devolvia riesgo residual por `session.idle` a 60s | `sendAndWait()` usa 60000ms por defecto y algunas respuestas reales terminaban despues de emitir contenido | Regla: fijar un timeout de espera mas amplio para fases planner/executor/synthesis cuando el router use SDK real |
| 2026-03-18 | El VSIX empaquetaba binarios y artefactos no Windows del Copilot CLI | `.vscodeignore` no filtraba prebuilds/ripgrep multiplataforma ni `.pdb` de debug | Regla: si la extension solo soporta Windows, excluir artefactos `darwin/linux/arm64` y `.pdb` del empaquetado para bajar peso sin tocar runtime |
| 2026-03-18 | Bundlear solo el runtime JS mejora arranque pero casi no mueve el peso final del VSIX | El tamaño esta dominado por `node_modules/@github/copilot*`, no por `extension.js`/`copilot_router.js` | Regla: usar bundling para reducir superficie y cold-start, pero atacar binarios pesados empaquetados si el objetivo principal es bajar MB de distribucion |
| 2026-04-16 | `skills_manager.py task-run` fallaba en Linux durante auditorias basicas | `_normalize_shell_command()` convertia atajos Unix a PowerShell y `cmd_task_step()` ejecutaba siempre con `_execute_powershell()` y fallback `cmd /c`, asumiendo Windows | Regla: resolver shell por plataforma en `task-step`; solo traducir a PowerShell en Windows y usar `bash` o `sh` para ejecucion/autorreintentos POSIX |
| 2026-04-17 | La recarga del catálogo de modelos podia romperse tras bundlear la extension | `extension.runtime.js` usaba `require.resolve("./free-models-catalog")`, pero el build con esbuild empaqueta el modulo y deja una advertencia/riesgo en `dist/extension.cjs` | Regla: cuando un runtime bundled necesite recarga dinamica, resolver la ruta absoluta al archivo fuente real y no depender de `require.resolve` sobre modulos internos del bundle |
| 2026-04-17 | El indicador visual del proveedor/modelo podia desaparecer solo en la extension instalada | `.vscodeignore` excluye `src-js/**`, pero el runtime empaquetado intentaba leer `src-js/free-models-catalog.js`; si esa carga fallaba, la activacion podia romperse y no se llegaba a crear la barra de estado | Regla: ningun comportamiento critico del runtime empaquetado debe depender de archivos excluidos del VSIX; mantener fallback interno o empaquetar explícitamente esos recursos |
| 2026-04-17 | La integracion global de VS Code parecia “rota” aunque el soporte ya existia | `skills_manager.py` podia escribir user settings globales, pero la extension solo exponia instalacion por workspace y fallaba si no habia carpeta abierta | Regla: si un capability global ya existe en el runtime, exponerlo como comando directo en la extension y dar fallback claro desde chat/comandos cuando no haya workspace |
| 2026-04-16 | Los proveedores externos devolvian 400 por `maximum context length` | `src-js/api-provider-adapter.js` reenviaba `request.prompt` sin presupuesto de contexto, sin `max_tokens` defensivos y sin interpretar HTTP 400 remotos | Regla: todo adaptador OpenAI-compatible debe compactar el prompt antes del POST, fijar presupuestos de salida y traducir errores de contexto a mensajes accionables |
| 2026-04-18 | El router Copilot podia disparar errores internos de prioridad del SDK/CLI (`No lowest priority node found`) | La extension permitia ejecuciones solapadas de `runCopilotRouter()` desde comando y chat dentro del mismo host | Regla: serializar o bloquear corridas concurrentes del router Copilot en `src-js/core/extension.runtime.js` antes de crear clientes/sesiones del SDK |
| 2026-04-18 | El agente podia incumplir checklist y trazabilidad por reglas inconsistentes | Las instrucciones mezclaban rutas heredadas (`.codex-agent`) con artefactos reales (`copilot-agent`) y el runtime del router no usaba `task-start`/`task-close` ni intake obligatorio | Regla: toda tarea no trivial debe arrancar con skill resolution + 3 aclaraciones minimas + `task-start`, y cerrar con `task-close` sobre el mismo `run_id` |
| 2026-04-18 | El catálogo visible de modelos gratis quedaba obsoleto aunque el adaptador ya soportaba modelos nuevos | `src-js/free-models-catalog.js` y el fallback de `src-js/core/extension.runtime.js` duplicaban listas estáticas separadas del adaptador externo | Regla: mantener una sola fuente de verdad del catálogo en el adaptador compartido y hacer que QuickPick/fallback deriven siempre de esa misma exportación |
| 2026-04-19 | Los wrappers OpenClaw divergían entre CMD y PowerShell y hacían más frágil el arranque del gateway | `openclaw-start.cmd` y `free-jt7-gateway-autostart.ps1` resolvían OpenClaw por separado con supuestos distintos | Regla: cuando exista un wrapper operativo común, reutilizarlo desde scripts auxiliares en lugar de duplicar la detección de binarios o repos locales |
| 2026-04-19 | El runtime mostraba capacidades ya existentes como si siguieran ausentes y además tenía huecos reales de integración | El análisis 08 quedó desfasado; además el router delegaba a proveedor usando `_pr` antes de inicializarlo, el scheduler existía pero no arrancaba y `nightlyTrain` referenciaba `cfg` fuera de scope | Regla: antes de implementar desde un documento histórico, recalcular el delta contra el código real y cerrar primero los huecos de integración verificables del runtime |
| 2026-04-19 | La compresion de contexto podia medir mal tokens y degradarse al compactar steps del router | `ContextHierarchy.pushHotMemory()` guardaba objetos crudos; luego `_compress()` operaba como si `content` fuese string, lo que sesgaba métricas y podia romper reglas de compaction | Regla: toda capa de memoria/contexto debe serializar entrada a texto mediante un servicio compartido de budget antes de estimar tokens o aplicar compresion |
| 2026-04-19 | El fix del error `No lowest priority node found` podia quedar incompleto aunque la extensión pareciera protegida | El guard de concurrencia estaba solo en `src-js/core/extension.runtime.js`; la entrada directa por `copilot_router.js`/`src-js/core/copilot_router.runtime.js` seguía pudiendo solaparse dentro del mismo proceso | Regla: toda protección contra concurrencia del router Copilot debe vivir en el core del router y la extensión solo puede añadir defensa adicional, nunca ser la única barrera |
| 2026-04-19 | La “configuración global” de Free JT7 seguía atada al checkout del repo aunque la extensión ya estuviera instalada globalmente | `skills_manager.py` agregaba nuevas rutas de instrucciones/agentes, pero no limpiaba las rutas stale del checkout anterior; además el runtime seguía pudiendo caer a `copilot` por defaults implícitos | Regla: la autocuración global debe podar primero cualquier ruta Free JT7 antigua en user settings, reescribirlas hacia la extensión instalada y usar OpenRouter como default operativo cuando no exista configuración explícita |
| 2026-04-20 | El selector de agentes de Copilot Chat puede ignorar agentes custom aunque el directorio `.github/agents` exista | `openclaw.agent.md` y `skill-creator.agent.md` usaban frontmatter incompleto o tools no soportadas, y además los settings reparados no activaban `github.copilot.chat.cli.customAgents.enabled` ni `github.copilot.chat.switchAgent.enabled` | Regla: todo `.agent.md` debe declarar al menos `name`, `description`, `tools`, `argument-hint` y `user-invokable` con tools soportadas, y la autocuración/instalación debe encender explícitamente los flags del selector de agentes |
| 2026-04-20 | La extensión seguía comportándose como repo bootstrapado aunque el VSIX estuviera bien instalado | El runtime arrancaba bridge/scheduler/plugin discovery sobre cualquier carpeta abierta y el instalador global reutilizaba `install` con target físico, mezclando settings globales con bootstrap de workspace | Regla: diferenciar siempre "extensión instalada" de "workspace gestionado"; la configuración global solo debe tocar settings de usuario y el estado operativo debe caer a globalStorage salvo que existan marcadores explícitos de Free JT7 en el workspace |
| 2026-04-20 | La autocuración global podía saltarse una reparación necesaria aunque los settings reales siguieran apuntando al checkout fuente | `ensureGlobalVsCodeSettings()` solo comparaba versión/path guardados en `syncState`; si `settings.json` se desviaba después, la extensión instalada no volvía a reparar | Regla: toda autocuración basada en estado recordado debe revalidar también el estado real del archivo/configuración objetivo antes de asumir que no hace falta actuar |
| 2026-04-20 | VS Code cargaba Free JT7 fuera del repo pero seguía ignorando parte de la configuración global | Se escribía `chat.agentFilesLocations` con una ruta absoluta al directorio `.github/agents` de la extensión instalada, formato que el renderer rechaza con `Skipping invalid path` | Regla: en settings globales de IDEs tipo VS Code no escribir `chat.agentFilesLocations` hacia la extensión instalada; reservar esa clave para rutas relativas de workspace y podar entradas Free JT7 stale durante la autocuración |
| 2026-04-20 | `canva-mcp 1.0.0` no funcionaba con ninguna versión publicada de `mcp` | El paquete mezcla APIs: crea `mcp.server.Server` pero usa `@mcp.tool()`, decorador que pertenece a `FastMCP` | Regla: antes de intentar resolver incompatibilidades Python solo con pinning, inspeccionar el código instalado del paquete para confirmar si el fallo es de versión o un bug upstream de API; si es lo segundo, documentar la imposibilidad del pinning puro y aplicar parche mínimo o fork controlado |
| 2026-04-20 | El pipeline de video falló aunque `remotion` estaba instalado | El subproyecto tenía la librería `remotion`, pero no el paquete que expone el binario CLI usado por `npm exec remotion render` | Regla: cuando un flujo invoque Remotion por CLI, instalar explícitamente `@remotion/cli` además de `remotion`; no asumir que la librería principal publica el ejecutable |
| 2026-04-20 | La validación viva de Canva seguía bloqueada aunque el usuario eligiera `.env.free-jt7` | `tools/design_agent/config.py` solo leía `os.environ` y no hidrataba las claves `CANVA_*` desde el archivo local ignorado que el propio repo propone | Regla: si el repo expone un archivo local de secretos como `.env.free-jt7`, el runtime debe cargar explícitamente solo las claves necesarias y sin sobreescribir variables ya exportadas |
| 2026-04-21 | Canva mostraba "client id no valida" al abrir OAuth del design agent | `tools/design_agent/canva_service.py` construia el flujo con `www.canva.com/api/oauth/*`, pero el cliente real se registra y autentica por `mcp.canva.com` | Regla: para clientes Canva MCP, forzar siempre `https://mcp.canva.com/authorize` y `https://mcp.canva.com/token` antes de construir `CanvaAuth` y cubrirlo con una prueba de regresion sobre la URL de autorizacion |
| 2026-04-22 | La selección de proveedor externo parecía seguir gastando Copilot aunque la llamada real ya salía por OpenRouter | `runCopilotRouter()` resolvía CLI/auth y construía `model_resolution` como `github-copilot-sdk` antes de entrar en la rama externa; además el chat participant corre dentro de GitHub Copilot Chat y ese host puede seguir contabilizando uso aunque el adapter externo haga la solicitud real | Regla: resolver `apiProvider`/`apiProviderModel` antes de cualquier preparación de la ruta Copilot, persistir `execution_route` real para la rama externa y advertir explícitamente cuando una invocación se hace desde el host Copilot Chat |
| 2026-04-22 | Bottles no pudo crear bottle para MT5 en Linux sin sudo aunque estaba instalado | `bottles-cli` forzó `offline mode` y falló en `Missing essential components` / `No managed runners found`, bloqueando aprovisionamiento automático de bottle | Regla: en Linux sin sudo, probar Bottles primero; si CLI entra en offline sin runners gestionados tras 2-3 intentos, aplicar fallback inmediato a Wine con `WINEPREFIX` de usuario y validar con `terminal64.exe` activo |
| 2026-04-24 | El panel reintentaba 429 y errores de configuración como si todos fueran transitorios | `SessionEngine` reencolaba cualquier excepción y el adaptador no tipaba rate limit/auth/configuración, así que el usuario veía el mismo error bruto repetido varias veces | Regla: etiquetar los errores del proveedor con flags (`isRateLimitError`, `isUserActionRequired`, `isRetryable`) y permitir auto-retry solo para fallos explícitamente retryables |
| 2026-04-24 | Corregir el checkout local no cambió el comportamiento del chat en VS Code | El editor estaba ejecutando la extensión instalada en `~/.vscode/extensions`, así que recompilar `dist/` en el repo no actualiza por sí solo la instancia activa del chat participant | Regla: cuando el bug afecta a una extensión instalada, después de reconstruir el repo hay que empaquetar/reinstalar la VSIX activa y luego recargar la ventana del editor |
| 2026-04-24 | OpenRouter podía terminar en `error HTTP 200.` dentro del panel | El adaptador trataba cualquier `payload.error` truthy como fallo, incluso cuando el proveedor devolvía una respuesta 200 con contenido válido o estructurado en `choices[0].message.content` | Regla: en respuestas OpenAI-compatible, solo considerar `error` fatal si el payload es significativo y no existe texto útil extraíble; además soportar `content` string y array antes de degradar a error |
| 2026-04-25 | La VSIX seguía empaquetando secretos locales aunque `env api/**` estaba ignorado | `env api` es un archivo con espacio en el nombre, no un directorio; el patrón `env api/**` no lo excluye y `vsce` lo incluyó junto a `.env.free-jt7` | Regla: en `.vscodeignore`, excluir tanto el archivo exacto (`env api`, `.env.free-jt7`, `.env`) como cualquier glob derivado; no confiar en patrones de directorio para secretos locales con nombres atípicos |
| 2026-04-25 | El chat parecía conversacional pero el modelo seguía respondiendo como si cada turno fuera nuevo | El panel solo reconstruía burbujas desde `tasks`, el `SessionEngine` no persistía `chatHistory`, el `chat participant` ignoraba `chatContext` y la rama de proveedor externo solo enviaba el último prompt sin contexto ni inspección local mínima | Regla: toda ruta conversacional debe persistir historial por sesión, pasar snapshot al proveedor/runner y reutilizar el contexto del host; si el prompt trae rutas locales existentes, inyectar un resumen automático del filesystem antes de pedir más datos |
| 2026-04-25 | La interfaz chat-first podía sonar como el proveedor base y saltarse capacidades reales de Free JT7 | El panel encolaba tareas directas al `SessionEngine` sin resolver skills ni abrir trazabilidad, y la identidad base del prompt no prohibía expresamente que modelos externos se presentaran como su marca | Regla: toda interacción del panel debe prepararse con intake asumido + skill resolution + `task-start/task-close`, y el `systemPrompt` debe obligar a responder como `free jt7`, no como MiniMax/OpenRouter u otra marca del proveedor |
| 2026-04-26 | El nuevo interfaz seguía pareciendo “agente” aunque en realidad estaba en modo LLM directo | El panel usaba el mismo flujo visual de chat para dos rutas distintas: ejecución real del agente y llamada directa al proveedor; sin una separación explícita, la mayoría de mensajes caían en la ruta sin herramientas | Regla: el panel debe exponer y persistir un `executionMode` explícito (`agent` vs `direct`), dejar `agent` por defecto y forzar la ruta Copilot real cuando el usuario espere capacidades completas de Free JT7 |
| 2026-04-26 | Una VSIX puede seguir “dependiendo” del chat de Copilot aunque el panel ya resuelva la mayor parte del flujo | Si `extensionDependencies`, `onChatParticipant:*` o el flag por defecto del participant siguen activos, la distribución continúa anclada a la experiencia de Copilot Chat aunque exista un panel autónomo | Regla: si el objetivo es panel-first, quitar la dependencia dura del host de chat, activar por startup y dejar el `chat participant` como capacidad opcional, no como ruta principal |
| 2026-04-26 | La primera integración de PDF prometía más de lo que podía verificar | `pdf-parse` falló en este entorno por incompatibilidad/fragilidad del runtime y no era una base confiable para una tool MCP operativa | Regla: para capacidades de documento en agentes locales, priorizar extractores del sistema (`pdftotext`) y dejar un fallback heurístico verificable antes que depender de una librería que no pase smoke real |
| 2026-04-26 | Un smoke de extensión instalada puede fallar aunque el código esté bien | La prueba se ejecutó en paralelo con `code --install-extension --force` y leyó la instalación antes de que terminara de asentarse | Regla: los smokes contra `~/.vscode/extensions` deben correr después de completar la reinstalación, no en paralelo con ella |
| 2026-04-26 | La VSIX instalada podía romper el router Copilot aunque el checkout local funcionara | `.vscodeignore` estaba excluyendo por completo `node_modules`, pero el runtime instalado aún necesita `@github/copilot-sdk`, `@github/copilot`, su binario por plataforma y dependencias mínimas (`vscode-jsonrpc`, `zod`) | Regla: si la extensión usa import dinámico o binarios en runtime, la VSIX debe incluir explícitamente ese subconjunto de `node_modules` y validarlo con un smoke sobre `~/.vscode/extensions` |
| 2026-04-26 | OpenClaw fallaba con `doctor --fix` usando proveedor custom CLŌD | `openclaw.json` declaraba `baseUrl` y `apiKey`, pero omitía `models.providers.clod.models`, campo requerido por el schema actual de OpenClaw | Regla: validar el `openclaw.json` generado con `openclaw config validate` y, para providers custom, declarar siempre al menos un `models[]` coherente con el modelo primario configurado |
| 2026-04-26 | El panel propio seguía heredando dependencia lógica de Copilot aunque el runtime externo ya existía | La interfaz webview usaba `freejt7.apiProvider` global y aceptaba `copilot` como proveedor, mezclando la configuración del host chat con la de la UI propia | Regla: separar la configuración del panel en claves propias (`freejt7.panel.*` en `globalState`) y tratar GitHub Copilot Chat como integración opcional, no como fuente de verdad del panel |
| 2026-04-26 | `runtime-audit` seguia fallando en Linux con recetas PowerShell y `python --version` | La normalizacion cross-shell solo traducía Unix -> PowerShell en Windows, pero no PowerShell -> POSIX en Linux; además asumía `python` aunque el host expone `python3` | Regla: toda receta de auditoria debe normalizar comandos en ambos sentidos y agregar fallback `python3` cuando `python` no exista |
| 2026-04-26 | El modo `agent` externo podia quedarse sin herramientas si faltaba OpenClaw o la API key | La unica ruta con tools para proveedores externos era OpenClaw; no existia un runner local minimo para inspeccion/verificacion determinista | Regla: mantener un fallback local de agente con herramientas seguras para lectura, inventario y verificacion basica, y tratar Copilot/OpenClaw como motores opcionales |
| 2026-04-26 | `package:local` del MCP reportaba 3 vulnerabilidades npm | El lockfile retenia transitivas vulnerables de `@modelcontextprotocol/sdk`: `@hono/node-server@1.19.11`, `hono@4.12.7` y `path-to-regexp@8.3.0` | Regla: cuando `npm audit` reporte vulnerabilidades transitivas con fix disponible, actualizar primero el lockfile, sincronizar `node_modules` y revalidar `npm audit`, smoke MCP y `package:local` |
| 2026-04-26 | El dry-run del bootstrap de IDE propio mostraba un binario distinto al esperado | `runBootstrap()` validaba siempre `--ide-bin` con resolución real y en dry-run degradaba al `code` del sistema aunque el flujo quisiera simular VSCodium portable | Regla: cuando el modo sea `dry-run`, respetar explícitamente el `ideBin` inyectado por el caller y evitar re-resolver binarios del host |
| 2026-04-26 | El instalador rootless del `.deb` quedaba roto al ejecutar launcher | El launcher embebido asumía `/opt/freejt7-desktop` fijo; al extraer paquete en `~/.local/freejt7-desktop` no resolvía la ruta real | Regla: en launchers empaquetados, resolver `APP_ROOT` por prioridad (`FREEJT7_APP_ROOT`, ruta relativa al script y fallback `/opt`) para soportar instalación de sistema y fallback local |
| 2026-04-26 | El bootstrap local de toolchain RPM quedó corrupto al correr `package:rpm` e `install:rpm` en paralelo | Ambos procesos escribían `~/.freejt7-app/runtime/rpm-tools/rootfs` a la vez y dejaban estado parcial (`toolchain local incompleta`) | Regla: cualquier bootstrap compartido de dependencias debe usar lock de proceso y despliegue atómico (`rootfs.new` -> `rootfs`) para tolerar concurrencia |
| 2026-04-26 | En perfiles aislados el panel podía “no funcionar” aunque la UI cargara | `freejt7.panel.executionMode` podía quedar persistido en `direct`; en modo app aislada ese camino depende de proveedor/API key y aparenta bloqueo total | Regla: cuando `freejt7.app.standaloneMode=true`, forzar `executionMode=agent` en persistencia/runtime y deshabilitar `direct` en el webview para mantener una ruta operativa con fallback local |
| 2026-04-26 | El panel podía enviar/probar con un modelo equivocado al usar `Modelo manual` | `modelCustom` solo persistía en evento `change` (blur); al pulsar enviar/probar sin blur se reutilizaba `providerState.model` previo, y al cambiar proveedor se podía arrastrar un modelo manual viejo | Regla: en UI de webview, sincronizar `modelCustom` en `input`, persistir selección antes de acciones sensibles (`enqueue`, `provider.test`, `catalog.refresh`) y limpiar modo manual al cambiar proveedor |
| 2026-04-27 | `openclaw config validate` rechazó la configuración generada por Free JT7 tras ampliar metadatos | Se añadieron claves no soportadas por el schema actual (`meta.lastAuthProfile`, `meta.lastPolicyProfile`) | Regla: en `openclaw.json`, escribir solo claves explícitamente soportadas por schema validado; para telemetría extra, usar `routeMeta`/audit local y no `meta` arbitrario |
| 2026-04-27 | El panel webview quedó “muerto” (sin acciones ni modelos) pese a compilar la extensión | En `createPanelHtml` se inyectó `\n` dentro de un string JS embebido; al estar dentro de template literal, se materializó como salto de línea real y rompió el parseo del script cliente | Regla: en JS embebido dentro de template strings, usar `\\n` para secuencias literales y añadir smoke que extraiga y compile el script generado (`node --check`) |
| 2026-04-27 | El chat del panel podía quedar sin respuesta aunque la UI cargara y creara sesiones | `preparePanelTask` bloqueaba `task.enqueue` cuando fallaba trazabilidad/skill-resolution (`task-start`), y el error quedaba solo en Output (sin feedback en webview) | Regla: en panel runtime, toda falla de preparación debe degradar a modo no-bloqueante y publicar errores al webview (`panel.server.error`) para que el usuario vea causa inmediata |
| 2026-04-27 | `jt7_path_search` devolvia cero resultados aunque `rg` encontraba contenido | En Node `spawnSync("rg")` podia devolver `error: EPERM` junto con `stdout` valido; la tool descartaba stdout y caia a fallback vacio para busqueda por contenido | Regla: si una tool de busqueda recibe stdout valido debe procesarlo aunque `spawnSync.error` exista, y debe tener fallback JS para contenido cuando `rg` no sea confiable |
| 2026-04-27 | `package:local` fallo con `Extension entrypoint(s) missing` aunque `dist/extension.cjs` existia | `vsce` fallo al resolver dependencias bajo el empaquetado actual; ademas el VSIX incluia artefactos `dist-deb/` y `dist-rpm/` pesados | Regla: para este proyecto empaquetar con `--no-dependencies` manteniendo includes explicitos de runtime y excluir artefactos de distribucion nativa en `.vscodeignore` |
| 2026-04-27 | `task-start --ide own-ide` fallaba con IDE no soportado | `own-ide` es un perfil VSCodium/VS Code pero no estaba declarado como alias del resolvedor de modelos | Regla: los perfiles propios basados en VS Code deben normalizarse a `vscode` en `IDE_ALIASES` antes de resolver modelo/autenticacion |
| 2026-04-27 | Tareas `acp:opencode` quedaban completadas pero el panel mostraba solo `}` | El normalizador de OpenClaw resumía solo `summary/text/reply/outputs` y caía al último renglón del JSON bruto, ignorando `finalAssistantVisibleText` y `payloads[].text` donde estaba la respuesta real | Regla: cuando un backend agente devuelva payload estructurado, extraer primero texto final visible y arrays `payloads[].text`; además añadir fallback de lectura en UI/runtime para reparar estado persistido legado |
| 2026-04-27 | El chat del panel parecia “roto” al usar `runtime local` porque devolvia siempre la misma respuesta | `runLocalAgentTask()` estaba diseñado como auditoria fija del workspace y no usaba el objetivo para inferir lecturas o verificaciones; la UI tampoco avisaba claramente que estaba en backend local limitado | Regla: toda ruta local de agente debe generar respuesta orientada al objetivo con heuristicas minimas verificables y la interfaz debe etiquetar `runtime local` como modo limitado |
| 2026-04-27 | Tras reinstalar own-ide, el panel podia volver a quedar “roto” sin que faltara la VSIX | El perfil aislado preservaba estado stale del panel (`runtimeBackend=local`, modelo heredado fuera de catalogo y sin sesion activa), así que reabrir la IDE reutilizaba una configuracion vieja aunque la extension estuviera bien instalada | Regla: al abrir el panel en standalone/own-ide, sanear el estado persistido (backend/modelo/proveedor) y sembrar una sesion inicial para que el chat sea usable sin limpieza manual del perfil |
| 2026-04-27 | OpenClaw podia parecer instalado pero Free JT7 no lo encontraba en rutas locales reales | La deteccion de binario estaba acoplada a una ruta rigida (`OPEN CLAW/node_modules/.bin/openclaw`) y no contemplaba carpetas con espacios/variantes de nombre ni ubicaciones locales alternativas | Regla: la resolucion de binarios externos debe explorar candidatos normalizados (`open claw/open-claw/open_claw`) y varias rutas (`node_modules/.bin`, `.bin`, `bin`, raiz), manteniendo fallback a PATH y smoke dedicado |
| 2026-04-27 | El chat principal mostraba respuestas locales como bloques de log dificiles de leer | `runLocalAgentTask()` usaba el mismo `summary` para UI y evidencia tecnica, y `control-panel.js` pintaba el texto completo sin tratamiento de chat; durante el fix reaparecio el riesgo de regex `\n` sin escape en script embebido | Regla: separar siempre resumen visible de evidencia tecnica (`local.technicalSummary`) y cubrir cualquier cambio del webview con `control_panel_script_syntax_smoke` antes de cerrar |
| 2026-04-27 | El modo agente en own-ide no era realmente autonomo cuando OpenClaw quedaba bloqueado | Free JT7 reutilizaba el `sessionId` del panel en OpenClaw, lo que permitia locks entre solicitudes; ademas el perfil aislado podia mantener `freejt7.panel.policy.mode=mixed` aunque el usuario esperaba permisos autonomos | Regla: en runtime agente externo usar session-id por tarea (`runId/taskId`), limpiar locks stale antes de ejecutar/reintentar OpenClaw y forzar `policy.mode=autonomous` en perfiles standalone/own-ide |
| 2026-04-27 | El fallback local seguia comportandose como auditoria genérica ante solicitudes concretas del sistema | `runLocalAgentTask()` solo inferia lecturas/verificaciones y no tenia una via segura para acciones de sistema soportadas; por eso `instala git` acababa en texto genérico aunque Git ya estuviera presente | Regla: para intents operativos comunes, el runtime local debe mapearlos a acciones de sistema explícitas y verificables antes de caer a una respuesta de auditoría |
| 2026-04-28 | El modo agente podia degradar a fallback local aunque OpenClaw estuviera instalado y el provider configurado | `runOpenClawAgentTask()` preparaba config y locks, pero no levantaba ni sondeaba el gateway aislado de Free JT7; ademas `gateway health` dio falsos negativos mientras `gateway status` si reflejaba `RPC probe: ok` | Regla: antes de ejecutar `openclaw agent`, asegurar el gateway con autostart detacheado sobre `OPENCLAW_CONFIG_PATH/STATE_DIR` del runtime actual y usar `gateway status` como sonda de readiness compatible |
| 2026-04-28 | El fallback local respondia a la solicitud equivocada y empujaba `system_install` por historial viejo | `inferHeuristicActions()` evaluaba todo el prompt auditado, incluyendo historial conversacional previo; así `continua` o `porque no estas usando el agente` heredaban acciones de Git de turnos anteriores | Regla: las heuristicas operativas del fallback local deben evaluar la `Solicitud actual` enfocada, no el prompt auditado completo; ademas deben responder con causa operacional clara cuando el motor agente cae por gateway/auth/network |
| 2026-04-28 | El gateway de OpenClaw podia no arrancar aunque el provider actual fuera correcto | `openclaw.json` retenia `models.providers.clod` de una corrida previa; al iniciar con otro provider, OpenClaw seguia resolviendo ese provider custom y abortaba por `CLOD_API_KEY` faltante | Regla: al regenerar config OpenClaw, eliminar providers custom stale que no correspondan al provider activo antes de arrancar el gateway |
| 2026-04-28 | El panel seguia “pareciendo local” incluso cuando el problema real era del agente/modelo | La politica de fallback trataba casi cualquier error de OpenClaw como permiso para caer a `runLocalAgentTask`, sin comprobar si la solicitud era resoluble localmente | Regla: en modo agente externo, intentar primero `provider-direct` ante fallos operativos/transitorios y habilitar fallback local solo si el objetivo actual es determinista y resoluble con herramientas locales |
| 2026-04-28 | El modo agente podia romperse antes de invocar OpenClaw con `The argument 'args[n]' must be a string without null bytes` | La serialización conversacional podía arrastrar `\0` desde historial/contexto local y `buildOpenClawAgentArgs()` reenviaba el texto crudo al `spawn()` | Regla: toda cadena que vaya a `spawn()` o a prompts serializados para runtimes CLI debe sanearse explícitamente eliminando bytes nulos antes de construir los argumentos |
| 2026-04-28 | El provider directo podia “responder bonito” pero no ejecutar la accion pedida | Tras fallar OpenClaw, tareas como crear carpetas seguian la ruta `provider-direct` y el modelo describia un `mkdir` como si se hubiera corrido, sin tocar el filesystem | Regla: si la solicitud es una accion local determinista y segura, priorizar herramientas locales sobre provider directo y responder solo con resultado verificado |
| 2026-04-28 | La continuidad del agente se fragmentaba entre panel, router y runtime y degradaba el comportamiento “agent-first” | `provider-router` reserializaba el contexto mientras `extension.runtime` seguia tomando decisiones con strings crudos; Free JT7 no era claramente dueño de la conversacion | Regla: el armado del `conversationRequest` y la entrada principal del modo agente deben vivir en un runtime central (`executeTask()`), y los adapters de UI/router solo deben delegar y transportar estado |
| 2026-04-28 | El panel y el engine seguian “adivinando” la ruta de ejecucion del agente incluso con runtime propio | La decision operativa seguia repartida entre heuristicas del runtime y metadatos reconstruidos en router/UI; eso ocultaba si una tarea era local-first o si dependia de OpenClaw/provider | Regla: el runtime del agente debe exponer un `executionPlan` por tarea y el resto del stack debe persistirlo/mostrarlo, no reconstruirlo a posteriori |
| 2026-04-28 | Skills, tools y MCP seguian siendo contexto lateral y no plan operativo del agente | El panel y la trazabilidad ya conocian `selectedSkills`, pero el runtime no publicaba una vista unificada de capacidades por tarea, así que la interfaz no podia explicar con claridad cómo iba a operar Free JT7 | Regla: el runtime agent-first debe generar un `capabilityPlan` por tarea y el resto del stack debe limitarse a persistirlo y mostrarlo |
| 2026-04-28 | Una orden incompleta como `crea la carpeta` seguia cayendo en texto de auditoria generica | El fallback local reconocia el dominio de la tarea, pero no distinguia entre intent valido e intent incompleto para create-directory | Regla: cuando una accion local es identificable pero faltan parametros minimos, responder con la aclaracion exacta que falta en vez de una sugerencia generica de “concreta el archivo” |
| 2026-04-28 | El runtime propio seguia publicando planes pero no despachaba acciones locales explicitas | La derivacion de `actions` seguia escondida dentro del fallback local, así que el ownership real de ejecucion no estaba completamente del lado de Free JT7 | Regla: si una ruta local depende de heuristicas deterministas, el runtime agent-first debe generar y pasar `actions` explicitas en vez de delegar esa decision al fallback |
| 2026-04-28 | `continua` dependia demasiado del historial y no de un estado operativo persistido del agente | Aunque habia chatHistory y snapshots, faltaba una memoria corta de sesion con ultimo objetivo, ultimo resultado y ultima route plan | Regla: persistir `agentState` por sesion y usarlo para enriquecer prompts breves de continuidad antes de caer a reconstrucciones mas difusas desde historial |
| 2026-04-28 | La continuidad podia romperse tras reinicio aunque ya existiera `agentState` persistido | El engine seguia asumiendo que `agentState` previo estaba intacto; si faltaba, quedaba legado o una tarea se interrumpia, `continua` no reflejaba la ultima situacion real | Regla: al cargar `session-engine`, reconstruir `agentState` desde la ultima tarea persistida y actualizarlo tambien al recuperar o reencolar tareas |
| 2026-04-28 | La compatibilidad Copilot podia contaminar el flujo principal porque el router legacy seguia leyendo `freejt7.apiProvider` | `copilot_router.runtime` mezclaba el provider global agent-first con la ruta heredada, así que el entrypoint Copilot dejaba de ser una compatibilidad secundaria aislada | Regla: toda ruta legacy debe resolver sus flags/config desde su propio namespace (`freejt7.copilotRouter.*`) y solo aceptar overrides explicitos, nunca heredar la fuente de verdad del flujo principal |
| 2026-04-28 | Skills, MCP y tools nativos seguian visibles en `capabilityPlan`, pero sin un dispatch trace verificable | El runtime publicaba snapshots y listas, pero no dejaba claro hacia que destino operativo activaba cada capacidad ni esa evidencia llegaba al fallback local | Regla: cuando Free JT7 anuncie capacidades nativas, `capabilityPlan` debe incluir un `dispatch` provider-independent (`owner`, `dispatchTarget`, `trace`) y los runtimes ejecutores deben preservarlo en evidencia/verificacion |

## Historial de bloqueos complejos
- Pendiente: agregar entradas cuando haya bloqueos con 3+ intentos.

| 2026-05-19 | Existian duplicados de artefactos (`files (1)` y scripts de prueba alternos) que ensuciaban el arbol y la auditoria | Se conservaron durante iteraciones previas sin una limpieza final de snapshots temporales | Regla: despues de estabilizar pruebas, eliminar snapshots/duplicados no referenciados y mantener un unico script canonico por proposito |

| 2026-05-19 | El resumen previo pudo percibirse incompleto al no dejar una comparativa profesional reproducible | Habia limpieza de repo pero faltaba un artefacto evaluable y verificable para comparar capacidades del agente | Regla: cada evaluacion de madurez del agente debe incluir una matriz de puntuacion reproducible + test unitario minimo |

| 2026-05-19 | Una puntuacion sin roadmap concreto no ayuda a cerrar brechas operativas | La salida inicial media el gap pero no traducia el delta a backlog priorizado con KPIs | Regla: toda evaluacion comparativa debe emitir plan P0/P1 con objetivos de 30 dias y KPIs verificables |

| 2026-05-19 | La brecha no se cierra solo con roadmap textual; se necesita gate automatizado | Faltaba enforcement continuo de calidad en PR con pruebas repetidas anti-flake | Regla: toda mejora de nivel profesional debe terminar en quality gate ejecutable en CI y local con metricas |

| 2026-05-19 | El nombre de carpeta `copilot-agent` generaba confusion de dependencia funcional | El namespace historico de estado no estaba desacoplado semanticamente en la documentacion ni en defaults nuevos | Regla: usar `freejt7-agent` como default semantico y mantener fallback legacy solo por compatibilidad |
